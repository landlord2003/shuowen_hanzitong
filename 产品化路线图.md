# 说文解字汉字 App · 产品化路线图

> 当前状态（2026-09-08）：8105 字单文件 Web App，已完成 A 路线「字源」阶段（GlyphWiki CC BY-SA 2.1 JP，黑底白描拓片风）+ 完整时间轴占位（甲骨→隶五阶段「待补全」）+ B 路线海外伙伴管线（wikimedia_seal/scan_trace 自动抓取，本机 merge_bins.py 合并）。
>
> 产品定位：**大众综合汉字文化商品**（国学/书法审美 + K12 学习工具 + 普通文化轻量），「一字一页·分层卡片」形态，让不同人群各取所需。

---

## 🎯 商品完整性三维评估

| 维度 | 现状 | 缺口 | 优先级 |
|---|---|---|---|
| 字形演变图 | 字源(GlyphWiki) + 5 阶段待补全 | 甲骨→隶真迹（B 路线解决） | 🟡 P1 |
| 六书/本义/今义 | AI 生成，会意↔形声混淆约 1100 字未终审 | 人工终审 | 🔴 P0 |
| **发音层** | 仅文字拼音 | 注音/多音字/朗读音频 | 🔴 P0 |
| **检索/笔顺** | 拼音/部首(文字)/六书/分类/排序 已有；笔画数筛选缺；笔顺动画缺 | 笔画数筛选 + 笔顺动画 | 🔴 P0 |
| 字源故事/文化 | 无 | 造字理据 + 成语 + 诗词引用 | 🟢 P1 |
| 书法字体矩阵 | 待补真迹 | 楷行草隶篆 + 名碑单字 | 🟢 P1 |
| 互动/留存 | 无 | 每日一字/测验/收藏/分享卡 | 🔵 P2 |

---

## 🗂️ Sprint 规划

### Sprint 1 — P0 基础能力（K12 + 普通，数据基本现成）【✅ 已完成 2026-09-08】
- ✅ 数据已具备：`characters.json` 已含 `pinyin` / `radical` / `radical_name` / `stroke`(总笔画) / `fanqie`(反切)。
- ✅ **笔画数筛选**：新增「≤5 / 6-10 / 11-15 / 16+」筛选按钮，复用现有 `stroke` 字段。
- ✅ **部首检索**：已有（搜索框输入部首字或部首名命中），优化占位提示文案。
- ✅ **笔顺动画**：新增「笔顺」区块，加载 `data/strokes.min.json`（公开源 Hanzi Writer Data 等）；无数据时显示「待补全」并指向 `tools/gen_stroke_order.py`。
- ✅ **注音(bopomofo)展示**：卡片与每日一字下方显示注音符号（如「ㄕˊ」「ㄌㄩˊ」），由纯算法模块 `data/pinyin-utils.js`（声母+韵母+声调映射，零外部数据）转换，接入 `app-features.js` 渲染。


- ✅ **已交付构件**：`tools/gen_stroke_order.py`（笔顺抓取脚本，联网跑完生成 `data/strokes.min.json`）；`六书待复核清单_会意指事.csv`（会意1050+指事51=1101字，上线前人工终审清单）。
- ✅ 构建自验：`build_web.py` 重新生成 `index.html`，JS 自检 **0 非 ASCII 通过**；笔顺图改为**笔画轮廓填充 + 每笔顺序圆点标注**（解析 SVG path 的 `M` 起点坐标），正确体现书写顺序；修复 `gen_stroke_order.py` 笔顺源 URL（jsDelivr 实际路径为 `/{c}.json` 且需 URL 编码）。
- ✅ **笔顺全量抓取完成**：6846/8105（84.5% 覆盖），罕见字缺失（Hanzi Writer Data 未收录）可忽略；Sprint 1 全部交付物已落地并经构建自验（内联 JS 0 非 ASCII）。

### Sprint 2 — P1 内容与美学（普通 + 国学，差异化卖点）【🔄 进行中 2026-09-08】
- 字源小故事 + 成语 + 诗词引用（AI 生成 + 人工抽检，成本最低、杠杆最高）。
- 书法字体矩阵：公版碑帖单字切图（楷·九成宫/颜柳；隶·曹全碑；篆·峄山碑等），现代书法家作品禁用。
- 演变阶段解说文字：每阶段配「长啥样、代表什么」，不只放图。

- ✅ **演变阶段解说文字**：时间轴各阶段（字源/甲骨/金文/简牍帛书/篆文/隶书）新增事实性解说（拓片风说明 + 各体特征），待补全阶段亦显示说明，不再「只放图」。实现：`data/cultural-ui.js` 的 `ERA_DESC` + 内联渲染。
- 🔄 **字源故事 / 成语 / 诗词**：详情页「字源故事·成语·诗词」区块（`culturalHTML` 渲染）已接入。生成器 `tools/gen_cultural.py` 经本地 Ollama **qwen3:14b** 产出（零外部 API 成本）；**关键修复**：qwen3 为思考模型，须加 `think:false` 否则 `format:json` 退化成 `{}`，并加空结果重试。**两处关键修复**：①qwen3 为思考模型，须加 `think:false` 否则 `format:json` 退化成 `{}`；②**并发过载**：首跑 `--workers 8` 把 Ollama/GPU 撑爆，单请求排队超 90s 超时返回空，50 字仅 1 条有内容。已改为 `timeout=180s` + 空结果重试 + **resume 时空值条目也会重生成** + 并发降到 `--workers 6`，重跑验证单字 36s、内容完整。**全量 8105 字后台生成重启中（workers 6，预计 10+ 小时，完成自动通知）**，`data/cultural.json` 自动填充，App 零改动生效。
- 📌 书法字体矩阵（楷行草隶篆 + 名碑单字）仍为 P1 后续项，依赖公版碑帖切图，本回合未启动。

### Sprint 3 — B 路线并行（国学核心）
- 海外伙伴跑 `wikimedia_seal`/`scan_trace` 抓甲骨/金文/篆真迹 → 回传 `dataset.bin` → 本机 `merge_bins.py` 合并，App 代码零改动。

### Sprint 4 — P2 留存/传播【✅ 前端功能已于 2026-09-08 完成】
- ✅ **每日一字**：按年内第几天确定性选字，首页横幅展示大字+拼音+一句字源/本义；文化内容加载后自动刷新文案。
- ✅ **收藏笔记**：详情页「☆收藏」按钮 + localStorage 持久化；列表卡片星标 + 「★收藏夹」筛选按钮，一键只看已藏字。
- ✅ **单字分享卡**：详情页「分享卡」按钮 → canvas 绘制（字源图 GlyphWiki + 大字 + 拼音/六书 + 字源故事）→ 下载 PNG，可直接微信直发。
- ✅ **每日测验**：顶部「测验」按钮 → 随机 5 题（拼音/六书/部首），即时判分、点字可看详情、可再来一次。
- 📌 以上 4 项均为**纯前端、零外部依赖**，逻辑集中于外部模块 `data/app-features.js`（含动态模态框与注入样式），`build_web.py` 仅做 ASCII 接线；内联 JS 自检 0 非 ASCII 不受影响。
- 🔲 后续：与汉字知识库/IMA 打通知识图谱（需后端/外部服务，非纯前端，独立排期）。

---

## ⚠️ 三条硬约束（上线前必守）
1. **六书终审是上线前置**：会意/指事约 1100 字 AI 标错（会意↔形声混淆），不终审伤专业口碑。
2. **音频授权**：用公版朗读或自购 TTS，不接有版权真人录音。
3. **笔顺/书法数据**：只取公开规范或公版碑帖；闭源字典库（如汉典）不抓；现代书法家作品有版权禁用。

## 📌 数据来源与许可证
- 字源图：GlyphWiki「字源」CC BY-SA 2.1 JP（可商用，须署名 + 衍生同授权）。
- 甲骨→隶演变：中研院漢字構形資料庫 CC BY-SA 2.5 TW（待 B 路线补全，须署名）。
- 笔顺数据：Hanzi Writer Data / cjkvi 等公开源（抓取脚本 `tools/gen_stroke_order.py`，运行需网络）。
- 说文/段注/玉篇/广韵/康熙：公版（>100 年古籍）。
- 六书/本义/今义/演变/字源故事：AI 生成（自有资产），上线前需人工终审关键项。
