# 说文解字 App — 工作计划
## （2026-09-14 17:30 更新，供老吴审阅）

---

### 〇、状态核实（本次实测，非记忆）

- **版本同步**：本地 `HEAD` = Gitee `origin/main` = `8345aa7`（`git ls-remote origin` 实测）。
  注：`git branch -vv` 显示的 "ahead 32" 是沙箱内 `origin/main` tracking ref 陈旧的**假象**，非真实分叉。
- **字形二进制已同步**：`data/dataset.bin` 的 HEAD blob 与磁盘字节数完全一致（14,931,458 B），push 零风险。
  此前"工作树 15931 条 > 上次提交 14741 条、需补 `git add`"的担忧**作废**。
- **未提交改动**（工作区）：5 个已跟踪文件改动 + 一批新增（详见第一梯队）。
- **预览**：http://127.0.0.1:8941 在线（serve_8941.py，ThreadingHTTPServer，每次请求从磁盘读，改完硬刷新即见）。

---

### 一、P0+P1 看板（7 项 → 已完成 6 项）

| 编号 | 任务 | 状态 | 关键结果与证据 |
|---|---|---|---|
| P0-1 | 诗词全量核实 | ✅ | 展示诗句 94.6% 为真实典籍出处（2182/2306，含本字）；5.4% 无含本字真句→诚实留空 |
| P0-2 | 六书冲突回写 | ✅ | 全量复核冲突 = 0 |
| P0-3 | 成语精确清洗 | ✅ 本次完成 | 白名单 **30,895 条**（pwxcoo/chinese-xinhua 提取）；**移出 1,080 / 保留 1,978**，移入 `phrases` **可逆**，零删除 |
| P1-4 | 字源 story 抽检订正 | 🟡 主体完成 | 本轮机器抽检 8105 → 高置信 167 条，逐条人工判决：**真错 132 已按《说文》重写**、假阳性 26（勿改）、待复核 9、中置信 145。**遗留：数据勘误 5 字 + suspect 9 字** |
| P1-5 | 书法字体矩阵 | ✅ | 4 个 OFL 字体 + 详情页「书法欣赏」区块，cmap 缺字门控 |
| P1-6 | 权威诗词接入 | ✅ | 真实语料反哺 + CDN 接入（并入 P0-1） |
| P1-7 | 隶书补源 | ✅ | `clerical-script.ttf` + `clerical-qingliu.ttf` 入库，隶书阶段可渲染 |

**本轮新增（非原看板）**：
- **汉典真实覆盖统计**：逐字检索汉典.net 全量 8105 字，得汉典各书体"有字形"字数（甲骨 1120 / 金 2072 / 大篆 5055 / 小篆 5564 / 隶 2688 / 简帛 2420）；纠正了此前误用"缺口补全批次"（452/1832/2821）当全量的错误。
- **产品说明三卡片改版**：`index.html` 内嵌面板 + `README.md` 重排为「知识说明（六书+各书体起源）/ 数据来源与许可 / 统计（四列表）」。
- **P1-4 修订**：132 字 story 重写（`data/cultural.json`，备份 `cultural.json.bak_pre_p1_132`）。

---

### 二、下一步工作计划（四梯队，按优先级）

#### 🔴 第一梯队 · 成果落地（先做，阻塞最小、价值最高）

> 近 4 天的成果（产品说明改版、汉典真实覆盖、P0-3 清洗、P1-4 修 132 字）**全部还在本地未落地**。

1. **提交并推送当前工作区**（沙箱只 commit；push 需你本机）
   - **修改（已跟踪）**：`README.md`、`index.html`、`data/cultural.json`、`data/cultural-ui.js`、`data/idioms_removed.json`
   - **新增（建议入库）**：`data/idioms_whitelist.json`（0.45MB）、`tools/P1-4_余量抽检报告.md`、`fix_real132.cjs`、`verify_132.cjs`、复算脚本（`compute_*.mjs` / `scan_zdic_coverage.mjs` / `update_*.mjs`）、本计划
   - **不入库（可重建）→ 补 `.gitignore`**：`data/idioms_whitelist_src.json`（9.84MB 原始下载）、`data/p1_*.json`、`zdic_coverage_cache.json`
   - push 命令（你本机）：`cd D:\WorkBuddy\projects\说文解字 && git push`

#### 🟡 第二梯队 · P1-4 收尾（数据准确性，商业化前置）

2. **数据勘误 5 字（硬伤，优先）**：`匦`(=簋)、`颃`(=亢)、`渺`(=眇)、`驭`(=御)、`台`(=臺) 的
   `characters.json` `shuowen`/`trad` 字段指向了**别的字**的训释（story 已按字本身写准，字段待修）。
   → 影响知识卡的"《说文》原文/本义"展示，属**数据源 bug**，非 story 问题。
3. **suspect 9 字终审**：`坰 夯 皋 麦 亟 尿 先 寇 亘` —— 拉《说文》+ 汉典逐字对照定稿。
4. **（可选）余量二次分桶**：mid 145 + 680 候选余量中，对"《说文》仅列单一义符"的字再筛一轮，提高全量置信度。

#### 🟢 第三梯队 · 商业化准备（P0/P1 清空后启动）

5. 产品说明口径复核（汉典定位=内部研究索引，不接入商用）；License/署名页与来源声明一致性复核；上架前检查清单。

#### ⚪ 第四梯队 · P2 增值（非阻塞）

6. 飞花令小游戏（复用已接好的真实诗词语料）。
7. 学习计划 / 闯关模式。
8. 本地 Ollama 文化生成（⚠️ 需你先恢复 Ollama 推理，见风险）。

---

### 三、阻塞与风险

- 🟢 **沙箱 push 无 Gitee 凭据**：只影响 push，commit 可在沙箱做；push 需你本机执行。
- 🟡 **数据勘误 5 字未修**：`characters.json` 字段指向他字训释，会污染知识卡展示。
- 🟡 **suspect 9 字待终审**；mid 145 噪声大暂不动。
- 🔴 **Ollama 推理挂死**：`/api/generate` 卡死（双模型、GPU/CPU 双路径同时挂），沙箱无法修，影响 P2 本地文化生成。需你本机重启机器/重置 GPU 驱动/重装 Ollama。
- 🟢 **仓库根目录调试残留**：`_fix*.py`/`_dbg*.py`/`_t*.js` 等已被 `.gitignore` 忽略（不入库），但物理上仍占目录，可择机清理。

---

### 四、给老吴的立即决策点（三件事）

1. **是否现在做第一梯队**（我 commit + 补 .gitignore，你本机 push）？—— 建议：**做**。
2. **第二梯队先修哪个**：数据勘误 5 字（硬伤）还是 suspect 9 字终审？—— 建议：**先修数据勘误 5 字**。
3. **余量二次分桶（4）要不要跑**？—— 可延后，不阻塞商业化。
