# 说文解字 · P0/P1 执行手册（2026-09-11）

> 目的：本机/沙箱职责分诊。沙箱网络与字体源受限（GitHub raw/镜像均 502、字体库不可达），本手册明确哪些已做、哪些需你本机执行。

---

## 一、沙箱已完成（无需你操作，已 commit 待 push）

| 项 | 结果 |
|---|---|
| **P0-2 六书 10 字冲突** | 复检 `characters.json.liushu` vs `cultural.json.story`，**全量冲突 = 0**（旧审计"10 字冲突"为历史态，已对齐，无需再动）。 |
| **P0-3 成语 conservative** | `tools/clean_idioms.py` 改为**仅英文数字判定**（古成语绝不含，零误删），重跑移出 **0**；`idioms=3058` 全保留。 |
| **P0-1 诗词 curated 核实** | `recover_poem_sources.py` 的 `VERIFIED` 语料扩充至 ~500 条，重跑写回 **177/2306 条真实出处（7.7%）**；剩余 2129 条待核（89.3%）。 |
| **P0-1 脚本去依赖** | `tools/verify_poems.py` 的 `opencc` 硬依赖改为可选（未装则跳过繁简），本机有数据即可跑。 |

---

## 二、需你本机执行（沙箱 GitHub/字体源不可达）

### 🔴 P0-1 诗词全量核实（目标待核率 < 20%）
1. 准备 chinese-poetry 数据集（CC0/MIT，数万首唐诗宋词诗经）：
   ```bash
   # 方式A：git 拉取（本机网络可达）
   git clone --depth 1 https://github.com/chinese-poetry/chinese-poetry.git /tmp/poetry
   mkdir -p .cache/poetry_src/json .cache/poetry_src/other
   cp /tmp/poetry/poetry/*.json .cache/poetry_src/json/
   cp /tmp/poetry/shijing/*.json .cache/poetry_src/other/
   # ci（宋词）为 json 数组，可直接放 json/ 或转 sqlite（脚本两种都支持）
   cp /tmp/poetry/ci/*.json .cache/poetry_src/json/
   ```
   若 GitHub 慢，可用镜像：`https://kkgithub.com/chinese-poetry/chinese-poetry` 或 Gitee 导入。
2. 安装 opencc（可选，繁简转换更准）：`pip install opencc-python-reimplemented`
3. 运行：`python tools/verify_poems.py --write`
   - 输出命中数；预期把待核率从 92% 降到 < 20%。
   - 未命中（AI 编造句）保持"待核"，前端诚实标注。

### 🟡 P0-3 成语精确清洗（成语 vs 俗语）
1. 准备成语白名单 `idioms_whitelist.json`（list[str]，从汉典/成语词典导出真成语集合）。
2. 运行：`python tools/clean_idioms.py --dict idioms_whitelist.json`
   - 不在白名单的 4 字短语移入 `phrases` 字段（**不删，可逆**），生成 `data/idioms_removed.json` 备查。
   - 沙箱 conservative 模式（无 `--dict`）已验证零误删，可放心。

### 🟡 P1-5 书法字体矩阵 + 🟢 P1-7 隶书补源
- 取公版名碑字体：**楷·九成宫/颜柳、隶·曹全碑、篆·峄山碑**；及一版**干净可商用隶书字体**（OFL/CC）。
- 落入 `data/fonts/`：甲骨/金（cluesurf/mark OFL，已入）、篆（崇羲，已入）、补 `clerical-script.ttf` 即自动补唯一 0% 阶段。
- `build_web.py` 的 `STAGE_FONTS` 网关自动生效，无需改代码。
- 单字切图（详情页"书法"区块）后续用 Pillow 从字体渲染，属 P1 后续。

### 🟡 P1-4 字源 story 抽检
- 需汉典/字源知识源（沙箱不可达）。建议本机逐批抽 200 字核本义/造字理据，错误回写 `story` 字段。

---

## 三、提交说明
- 沙箱改动已 `commit`（本地 `e3bc521` 之后），但 **push 需你本机**（沙箱无 Gitee 凭据）。
- 本轮涉及文件：`tools/recover_poem_sources.py`（扩充语料）、`tools/verify_poems.py`（去 opencc）、`tools/clean_idioms.py`（零误删 conservative）、`data/cultural.json`（177 条出处 + idioms 零误删）、`data/cultural-ui.js`（缓存 `?v=20260911f`）、`P0_P1_执行手册.md`、本报告。
- 本机 push 前：
  ```bash
  cd "D:/WorkBuddy/projects/说文解字"
  git add -A && git commit -m "P0: 诗词curated核实177条+六书0冲突+成语零误删conservative" && git push -u origin main
  ```
  （`git add -A` 安全：本轮无临时文件；若想排除 `data/cultural.json.bak_*` 备份，先 `git status` 确认。）
