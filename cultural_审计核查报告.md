# cultural.json 审计核查报告（v2 · 2026-09-11 更新）

> 审计对象：`data/cultural.json`（字源文化故事库，平铺字典 `{"字":{story,idioms,poems}}`）
> 本轮触发：用户要求「把 cultural 诗词真出处抓回 + 审计数据可信度」。
> 配套动作：`tools/recover_poem_sources.py`（权威语料精确匹配写回）、`data/cultural-ui.js`（✓ 已核实标记）。
> 上一版（2026-09-10）结论见文末附录。

---

## 一、审计结论（verdict）

| 维度 | 本轮结果 | 说明 |
|---|---|---|
| **完成度** | 🟢 通过 | 8105/8105 全覆盖，无缺字、无空值、无错误标记。 |
| **诗词出处·原状态** | 🔴 高危 | 2504 条 `poems` 的 `source` **全部 =「待考」**，0 条有真实出处（教学误人子弟风险）。 |
| **诗词出处·抓回后** | 🟡 部分修复 | 经权威语料精确匹配，**268 条（10.7%）已写回真实出处**，带前端「✓」标；2236 条仍待核。 |
| **诗词内容·编造率** | 🔴 高危 | 60 条抽样：约 50% 为可确证的真实古诗名句，约 42% 为 AI 编造/俗语套话，约 8% 真句被拼接篡改。 |
| **六书一致性** | 🔴 不通过 | 10 字 `story` 明示六书与人工终审 `data.liushu` 冲突（story 错标）。 |
| **成语真实性** | 🟡 风险 | 成语字段混入 89 条非成语/现代短语。 |
| **故事长度** | 🟢 偏短 | 中位 52 字（32–77），可作字源一句话。 |

**一句话**：数据**不缺不坏、生成真跑完**；但诗词模块**可信度低**——仅 10.7% 已核实可放心展示，89.3% 仍待核（含相当比例 AI 编造句）。上线前必须对未核实诗句做诚实标注（已做：前端统一「示例·出处待核」），且 P1 须接入权威古诗库做全量核实。

---

## 二、诗词可信度量化（核心）

- 总诗句：**2504 条**；原 `source` 状态：**2504/2504 = 100% 待考**。
- 抓回方式：`tools/recover_poem_sources.py` 内置**高置信度权威语料库**（诗经/楚辞/论语/乐府/古诗十九首/唐诗/宋词/诸子/医典等约 300 条可确证名句 → 出处），对每条做**归一化精确匹配**（去标点空白逐字比），命中即写回「作者·《作品》」，**未命中保持待考、绝不补造**。
- 抓回结果：**268 条已核实（10.7%）**，覆盖杜甫/李白/王维/苏轼/曹操/陶渊明/诗经/论语/西窗烛级名篇等。
- 抽样核查（60 条人工分类）可信度画像：
  - ✅ 真实可确证名句 ≈ 30 条（50%）：如「一去紫台连朔漠…」(杜甫)、「欲穷千里目…」(王之涣)、「工欲善其事…」(论语)、「气蒸云梦泽…」(孟浩然)。
  - 🟡 真句被拼接/截断 ≈ 5 条（8%）：主干真、后缀 AI 补（如「少年负壮气…但使中圣人…」仅前半真）。
  - ❌ AI 编造/俗语套话 ≈ 25 条（42%）：如「巨灵劈山开道路」「勘破禅机」「养晦待时」「膝上琴横…」等，古籍中不存在。
- **推导**：真实名句在全集占比约 50%，但仅约 1/5（名篇级）能被 curated 语料自动核实；其余真实但冷僻句 + 全部编造句共 **2236 条（89.3%）须靠权威库全量匹配或诚实标注**。

---

## 三、已落地修复（本机已完成）

1. **真出处抓回**：`tools/recover_poem_sources.py` 写回 268 条真实出处 → `data/cultural.json`。
2. **前端诚实标注**：`data/cultural-ui.js` 诗句渲染——`source` 非待考显示「✓ 作者·《作品》」；待考显示「示例·出处待核」。预览服务 `http://127.0.0.1:8080` 已生效（HTTP 200，`?v=20260911d`）。
3. **缓存刷新**：bump `cultural.json?v=20260911c → d`，强制浏览器拉新数据（无需重建 `index.html`，cultural 为运行时 fetch）。

> 复用说明：扩 curated 语料（`VERIFIED` 列表）后重跑脚本即可线性增加核实条数；生产级修复见第四节 P1。

---

## 四、修复建议（移交下阶段）

| 优先级 | 项 | 动作 | 依赖 |
|---|---|---|---|
| 🔴 P0 | 诗词全量核实 | **接入权威古诗库**（中华诗词库/古诗文网公开 API 或本地 CC0 诗库）对 2236 条待核做全量匹配；命中写回、未命中标「示例」。脚本可复用 `recover_poem_sources.py` 的匹配框架。 | 需可访问语料源（沙箱 GFW 可能需海外/用户侧取） |
| 🔴 P0 | 六书 10 字冲突 | 以 `characters.json.liushu` 为准回写 `cultural.json` 对应 `story` | 无 |
| 🟡 P1 | 成语字段清洗 | 剥离 89 条非成语/现代短语；俗语/诗句挪字段并改名 | 无 |
| 🟡 P1 | 字源故事抽检 | P1-4 抽检 `story` 本义/造字理据，错误回写 | 无 |
| 🟢 P2 | 扩 curated 语料 | 人工增补可确证名句（乐府/宋诗/清诗/诸子更多）重跑 | 无 |

**纪律红线**：面向教学，**宁可标「示例·出处待核」也不得保留/补造虚假出处**（授权与准确性红线）。

---

## 附录：v1（2026-09-10）已记录项

- 完成度/结构/六书冲突 10 字/成语 89 条污染/诗句 5/5 编造抽样/故事偏短 —— 见本文件历史版本，结论未变，仅诗词出处由「100% 待考」推进到「10.7% 已核实」。


## v2.1 补充修正（2026-09-11）

### 1. 缺形措辞改正（呼应老吴反馈）
- 原详情页/说明文案写「历史上不存在此字该阶段字形」「历史上本无此形」，老吴指出表述不准确（断言"历史上本无"过于绝对、且未标注来源）。
- 全部改为谦逊归因口径：「**根据汉典查询，此字目前未发现该阶段字形**」（line 178/189/305）、「据汉典等字书暂未收录此形」（line 150）、「汉典等字书暂未收录其古形」（line 148 欢迎弹窗）。
- 重建 `index.html` 通过（`node --check` OK），预览已生效。

### 2. 诗词「不含本字」错配清洗（呼应老吴反馈：刀字诗词引用无"刀"字）
- 实测：2504 条 poems 中 **198 条（7.9%）诗句根本不含所挂汉字**（如「刀」挂「剑外忽传收蓟北…」、语义弱关联冒充字内引用）。
- 新增 `tools/clean_mismatch_poems.py`：严格契约「诗句须含本字」，错配一律移除（不编造关联）；移除清单落 `data/poems_removed_mismatch.json` 备查。
- 结果：**清洗后 2306 条，0 条错配**；「刀/刃/下/川/久/友/日/水/见/仇…」等错配已清除（如「刀」现 0 诗词，诚实空）。
- 仍遗留（上线前硬约束）：2236 条待核真出处（P1 接权威古诗库全量匹配）；被移除诗句如需重新正确挂到"含该字"的其它字下，属 P1 重索引任务，未在本轮范围。

### verdict（更新）
- 文案准确性：🔴→🟡 缺形表述已从绝对断言改为汉典归因（诚实）。
- 诗词准确性：🔴 不含本字错配已清零（🟢），但待核真出处仍 89.3%（🔴，P1 收口）。
